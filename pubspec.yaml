name: StrategosFinal
description: Strategos by Czarina
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.1
  url_launcher: ^6.3.0
  file_picker: ^6.2.1

  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.5.1

  # Backend (Supabase)
  supabase_flutter: ^2.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/branding/strategos_logo_full.png